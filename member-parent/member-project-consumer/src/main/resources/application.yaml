server:
  port: 5000
spring:
  application:
    name: projectConsumer
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  session:
    store-type: redis
  redis:
    host: localhost
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka



aliyun:
  oss:
    access-key-id: MyAccessKeyFromAliyun
    access-key-secret: MyAccessKeySecretFromAliyun
    bucket-domain: http://dcmcrowd.oss-cn-shanghai.aliyuncs.com
    end-point: http://oss-cn-shanghai.aliyuncs.com
    bucket-name: dcmcrowd